"""Агентное ядро с режимами чатов.

Каждый режим — свой системный промпт и свой набор инструментов:
  assistant  — полный набор (задачи, календарь, привычки, напоминания, всё остальное)
  translator — переводчик без инструментов, быстрая модель
  research   — веб-поиск + сохранение находок в задачи
  board      — «совет директоров» + веб-поиск
  business   — разбор бизнес-моделей + веб-поиск
  project    — единый агент проекта-клиента: все инструменты + досье из Notion;
               включается автоматически для любого чата внутри проекта,
               какой бы режим ни был у чата записан
"""
import logging
from datetime import datetime

from anthropic import AsyncAnthropic

import config
import tools
from services import notion as notion_service
from tools import context as tool_context

logger = logging.getLogger(__name__)

client = AsyncAnthropic(api_key=config.ANTHROPIC_API_KEY)

MAX_TURNS = 12

BASE_IDENTITY = """\
Ты Паола — персональный ассистент Юлии Йозефсон, независимого бизнес-консультанта
в Италии (Дезио). Позиционирование её бренда: «Prima i processi, poi l'AI»,
клиенты — итальянские PMI и семейные компании.
Говори по-русски, тон партнёрский и прямой, без подхалимства и воды.

Честность про возможности: ты НЕ умеешь создавать и прикреплять файлы (Excel,
PDF, Word и т.п.) — никогда не обещай файл и не говори, что «прикрепила» его.
Объёмные структурные материалы (таблицы, отчёты) сохраняй страницей в Notion
через инструмент save_to_notion (там, где он доступен) и давай ссылку; в чате —
только краткий вывод. Если инструмента нет — так и скажи и предложи вариант.

Честность про выполнение — железное правило, важнее краткости и «полезного» тона:
- Никогда не выдумывай. Не сообщай как факт то, чего не видела в результате
  инструмента; не отчитывайся о действии, которое не выполнила или которое
  завершилось ошибкой.
- Нет подходящего инструмента под просьбу — прямо скажи: «этого я сделать не могу,
  у меня нет такой функции», назови, что умеешь ближайшее, и предложи вариант.
  НЕ подменяй просьбу похожим действием молча; если делаешь похожее — сначала
  скажи, ЧТО именно сделаешь, и почему это не ровно то, что просили.
- Инструмент вернул ошибку или пустой/странный результат — так и доложи:
  какой шаг, что вернул, что это значит. Формулировки вида «видимо», «похоже,
  сработало» запрещены — либо ты видела подтверждение в результате инструмента,
  либо честно говоришь «не подтверждено».
- Не уверена, что поняла запрос или что результат точный (нашла не ту запись,
  совпадение неполное) — скажи о своей неуверенности до действия, а для
  необратимых действий (удаление, отправка) при неполном совпадении сначала
  покажи, что нашла, и дождись подтверждения.
"""

WEB_SEARCH_TOOL = {"type": "web_search_20260209", "name": "web_search", "max_uses": 8}

_TOOL_BY_NAME = {t["schema"]["name"]: t["schema"] for t in tools.ALL_TOOLS}


def _pick(*names: str) -> list[dict]:
    return [_TOOL_BY_NAME[n] for n in names if n in _TOOL_BY_NAME]


MODES: dict[str, dict] = {
    "assistant": {
        "model": config.MODEL_SMART,
        "tools": [WEB_SEARCH_TOOL] + tools.SCHEMAS,
        "system": BASE_IDENTITY + """
Это главный чат-ассистент. У тебя есть реальные инструменты: задачи и календарь,
привычки, напоминания, переводы, веб-поиск, совет директоров — используй их,
а не рассказывай, что «нет доступа». Действия с данными (создать/закрыть/удалить/
перенести задачу, отметить привычку) выполняй сразу, если смысл ясен; уточняй только
при реальной двусмысленности. «Удалить/убрать задачу» — это delete_task (задача
исчезает), «закрыть/сделала/готово» — close_task (Done); не подменяй одно другим. Отвечай кратко на простое и обстоятельно на сложное —
кроме создания задачи: после create_task всегда показывай зону, проект, приоритет
и дедлайн из результата инструмента (как в примере ниже), даже если остальной
ответ короткий. Это не нарушает правило краткости — это и есть короткий ответ,
просто с полными полями, чтобы Юлия могла проверить занесение с одного взгляда.
Пример: «Готово — «Выбрать торшер в гостиную»: 🏠 Family, Личное, ✅ Обычное,
дедлайн 05.07.2026.»""",
    },
    "translator": {
        "model": config.MODEL_SMART,
        "tools": [],
        "system": BASE_IDENTITY + """
Это чат — языковой ассистент (итальянский / английский / русский). Определи
по сообщению Юлии, какой из четырёх режимов нужен:

1. Текст на итальянском или английском БЕЗ явной просьбы — это её черновик.
   Верни исправленную, естественную для делового общения версию на ТОМ ЖЕ языке,
   затем блок «Правки:» — по-русски кратко объясни каждую значимую правку
   (грамматика, лексика, тон). Если текст уже корректен — так и скажи,
   предложи максимум 1–2 необязательных улучшения.
2. Явная просьба перевести («переведи», «на английский» и т.п.) — сделай перевод
   и ответь ТОЛЬКО переводом, без пояснений. По умолчанию: русский → итальянский,
   деловой стиль; итальянский/английский → русский. Указанное направление/стиль
   важнее умолчаний.
3. Просьба помочь выстроить коммуникацию (написать письмо, ответить клиенту,
   сформулировать мысль) — отвечай на этот запрос: предложи готовый текст
   на нужном языке и при необходимости коротко поясни выбор формулировок.
4. Русский текст без просьбы — переведи на итальянский, деловой стиль,
   ответь только переводом.

Терминологию держи единой в рамках чата. Для терминов с несколькими вариантами
выбирай принятый в деловой практике Италии; спорный вариант можно пометить
альтернативой в скобках.""",
    },
    "research": {
        "model": config.MODEL_SMART,
        "tools": [WEB_SEARCH_TOOL] + _pick("create_task", "save_to_notion"),
        "system": BASE_IDENTITY + """
Это чат-ресерч. Задача — глубокое исследование тем по запросу с веб-поиском.
- Всегда указывай источники ссылками. Надёжные: Il Sole 24 Ore, ANSA, ISTAT,
  Bankitalia, Reuters, FT, Bloomberg, официальные документы ЕС. Агрегаторы и
  перепечатки — не источник.
- Структура ответа: краткий вывод → факты с цифрами и датами → источники →
  что это значит для Юлии/клиента.
- Объёмный результат (сравнительная таблица, отчёт) сохраняй через
  save_to_notion: полная версия — на странице Notion, в чате — краткий вывод
  и ссылка. В названии страницы укажи суть и проект.
- Если просят «сохранить» или «в задачи» — используй create_task.""",
    },
    "board": {
        "model": config.MODEL_SMART,
        "tools": [WEB_SEARCH_TOOL] + _pick("board_review", "save_to_notion"),
        "system": BASE_IDENTITY + """
Это чат «Совет директоров» — оценка бизнес-идей и решений.
- Когда Юлия описывает идею — вызывай board_review с полным контекстом идеи.
- Уточняющие вопросы после совета обсуждай сам(а), опираясь на выданные мнения;
  повторно собирай совет только по явной просьбе или для новой идеи.
- Если для оценки не хватает рыночных фактов — сначала проверь их веб-поиском.""",
    },
    "business": {
        "model": config.MODEL_SMART,
        "tools": [WEB_SEARCH_TOOL] + _pick("save_to_notion"),
        "system": BASE_IDENTITY + """
Это чат разбора бизнес-моделей и анализа рынка/конкурентов.
- Бизнес-модель разбирай структурно: ценностное предложение → сегменты клиентов →
  каналы → потоки выручки → структура издержек → ключевые ресурсы и партнёры →
  риски. В конце — 3 главных вопроса, которые надо проверить.
- Конкурентов и рынок проверяй веб-поиском: реальные игроки, цены, динамика.
  Цифры — с источниками.
- Объёмный результат (сравнительная таблица, отчёт) сохраняй через
  save_to_notion: полная версия — на странице Notion, в чате — краткий вывод
  и ссылка. В названии страницы укажи суть и проект.
- Всегда привязывай выводы к контексту: итальянский рынок, PMI, специфика клиента.""",
    },
    "project": {
        "model": config.MODEL_SMART,
        "tools": None,   # заполняется ниже: все инструменты + веб-поиск
        "system": BASE_IDENTITY + """
Это рабочий чат проекта-клиента. Ты — единый агент проекта: сама понимаешь,
какая перед тобой задача, и сама выбираешь инструменты, не спрашивая, «каким
режимом» работать:
- research с веб-поиском — всегда с источниками ссылками (надёжные: Il Sole
  24 Ore, ANSA, ISTAT, Bankitalia, Reuters, FT, Bloomberg, официальные
  документы ЕС; агрегаторы — не источник);
- разбор бизнес-моделей — структурно: ценностное предложение → сегменты →
  каналы → выручка → издержки → ресурсы и партнёры → риски, в конце 3 вопроса
  на проверку;
- оценка идеи или решения — board_review (совет директоров);
- задачи, календарь, напоминания — соответствующие инструменты;
- объёмный материал (таблица, отчёт) — save_to_notion: полная версия в досье
  клиента, в чате краткий вывод и ссылка.

Память проекта — досье клиента (приложено ниже). Это единственное, что ты
помнишь между чатами проекта. Узнала новый устойчивый факт — цифры, решение,
договорённость, предпочтение клиента — сразу и молча зафиксируй через
update_client_memory, не спрашивая разрешения. Никогда не переспрашивай то,
что уже есть в досье.
Строки досье вида «[сохранённый материал] <название>» — это подстраницы
Notion (транскрипции, заметки, отчёты), их полный текст не подгружен заранее.
Если для ответа нужно их содержимое — сама вызови read_material(title), а не
проси Юлию вставлять текст в чат; так только если материал прочитать не
удалось (пустой, это файл, а не текст).""",
    },
}

MODES["project"]["tools"] = [WEB_SEARCH_TOOL] + tools.SCHEMAS

# Консунтиво Generali — общий блок для чатов, где доступны инструменты учёта часов.
_CONSUNTIVO_GUIDE = """

Консунтиво Generali (учёт часов для фактуры). «Собери консунтиво за <период>»:
1) list_generali_events(start, end) — берёт встречи за период (в т.ч. прошедшие);
2) классифицируй каждую: код W#-F# → Ondata/Fase; тип CI→CoInd, SAL→Track,
   IntOpe→IntOpe, PrMa/PrOnd/PrUpd/Form/Cons/AnDa как есть, BBR→Progett, нет
   кода/типа → Progett; Reparto — по справочнику из результата, не уверена —
   оставь пусто; личные встречи без признаков Generali пропускай;
3) add_consuntivo_rows(rows) — строки лягут ЧЕРНОВИКОМ (Stato = TO CHECK), НЕ в
   фактуру, плюс соберётся отчёт-страница. ВАЖНО: если встреч много (целый
   месяц), заноси порциями — вызывай add_consuntivo_rows несколько раз по
   ~10 строк (например, по неделям), а не одним огромным вызовом, иначе ответ
   оборвётся по длине;
4) когда занесла ВСЕ строки периода — вызови consuntivo_report(start, end) один
   раз: соберётся отчёт-страница и уйдёт пуш.
Юлия проверяет и правит строки в базе сама. Только на её явное «подтверждаю
<период>» вызывай confirm_consuntivo(start, end) (TO CHECK → Confermato) —
никогда не подтверждай по своей инициативе. «Сколько часов/сумма за <период>» —
query_consuntivo(start, end). Даты считай сама из периода (сегодняшнюю дату
знаешь), формат YYYY-MM-DD."""
MODES["assistant"]["system"] += _CONSUNTIVO_GUIDE
MODES["project"]["system"] += _CONSUNTIVO_GUIDE


def _system_for(cfg: dict, project_name: str | None, project_desc: str | None,
                dossier: str = "", has_dossier_page: bool = False) -> str:
    now = datetime.now(config.ROME_TZ)
    weekdays = ["понедельник", "вторник", "среда", "четверг",
                "пятница", "суббота", "воскресенье"]
    system = cfg["system"] + (
        f"\nСегодня {now:%d.%m.%Y}, {weekdays[now.weekday()]}, "
        f"время {now:%H:%M} (Europe/Rome)."
    )
    if project_name:
        system += (f"\n\nЭтот чат относится к проекту «{project_name}». "
                   f"Весь анализ веди в контексте этого проекта.")
        if project_desc:
            system += f"\nОписание проекта: {project_desc}"
        if dossier:
            system += ("\n\n=== ДОСЬЕ КЛИЕНТА (страница проекта в Notion) ===\n"
                       f"{dossier}\n=== КОНЕЦ ДОСЬЕ ===")
        elif has_dossier_page:
            system += ("\n\nДосье клиента пока пустое — наполняй его через "
                       "update_client_memory по ходу работы.")
        else:
            system += ("\n\nДосье клиента не настроено (нет NOTION_CLIENTS_PAGE_ID) — "
                       "память между чатами проекта недоступна; если Юлия на неё "
                       "рассчитывает, честно скажи об этом и подскажи настройку.")
    return system


def _extract_text(content: list) -> str:
    return "\n".join(b.text for b in content if b.type == "text").strip()


async def run_chat(mode: str, messages: list[dict],
                   project_name: str | None = None,
                   project_desc: str | None = None,
                   project_page_id: str | None = None) -> str:
    """Прогнать историю чата через агентный цикл выбранного режима.

    Чат внутри проекта всегда идёт единым агентом «project» (все инструменты
    + досье клиента из Notion), независимо от режима, записанного у чата.
    """
    is_project = bool(project_name or project_page_id)
    cfg = MODES["project"] if is_project else MODES.get(mode, MODES["assistant"])

    dossier = ""
    if project_page_id:
        try:
            dossier = await notion_service.get_page_text(project_page_id)
        except Exception:
            logger.exception("Не удалось прочитать досье проекта %s", project_page_id)

    system = _system_for(cfg, project_name, project_desc,
                         dossier=dossier, has_dossier_page=bool(project_page_id))
    convo = list(messages)

    # инструменты берут страницу-досье из contextvar (изолировано на запрос)
    ctx_token = tool_context.CURRENT_PROJECT_PAGE.set(project_page_id)
    try:
        return await _agent_loop(cfg, system, convo)
    finally:
        tool_context.CURRENT_PROJECT_PAGE.reset(ctx_token)


async def _agent_loop(cfg: dict, system: str, convo: list[dict]) -> str:
    # режим без инструментов (переводчик) — один вызов
    if not cfg["tools"]:
        response = await client.messages.create(
            model=cfg["model"], max_tokens=4000, system=system, messages=convo)
        return _extract_text(response.content) or "(пустой ответ)"

    for _ in range(MAX_TURNS):
        response = await client.messages.create(
            model=cfg["model"], max_tokens=16000, system=system,
            tools=cfg["tools"], messages=convo)

        if response.stop_reason == "pause_turn":
            convo.append({"role": "assistant", "content": response.content})
            continue

        if response.stop_reason != "tool_use":
            text = _extract_text(response.content)
            if text:
                return text
            # Ответ без текста: если оборвался по длине — подскажем, а не молчим
            # «(пустой ответ)» (например, слишком большой вызов инструмента).
            if response.stop_reason == "max_tokens":
                return ("Ответ получился слишком длинным и оборвался. Если это "
                        "большая операция (например, консунтиво за целый месяц), "
                        "попроси собрать её по частям — по неделям.")
            return "(пустой ответ)"

        convo.append({"role": "assistant", "content": response.content})
        results = []
        for block in response.content:
            if block.type != "tool_use":
                continue
            handler = tools.HANDLERS.get(block.name)
            if handler is None:
                result, is_error = f"Неизвестный инструмент: {block.name}", True
            else:
                try:
                    result, is_error = await handler(block.input or {}), False
                except Exception as e:
                    logger.exception("Tool %s failed", block.name)
                    result, is_error = f"Ошибка инструмента: {e}", True
            results.append({
                "type": "tool_result",
                "tool_use_id": block.id,
                "content": str(result)[:50000],
                "is_error": is_error,
            })
        convo.append({"role": "user", "content": results})

    return "Я упёрлась в лимит шагов на этот запрос. Разбей задачу на части или повтори."
